import {} from "../types";

const initialState = {};

const EventReducer = (state = initialState, action) => {
    switch (action.type){
        default:
            return {...state}
    }
};

export default EventReducer;
